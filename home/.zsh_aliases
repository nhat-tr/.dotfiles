# Shortcuts
alias work="cd $HOME/Work"

# Intuitive map function
# For example, to list all directories that contain a certain file:
# find . -name .gitattributes | map dirname
alias map="xargs -n1"

# Other
alias n="nvim"
alias c="clear"
alias cc="clear"
alias refresh="$HOME/.dotfiles/scripts/linkconfigs.sh && tmux source ~/.config/tmux/.tmux.conf ; source ~/.zshrc"
alias dtf="cd $HOME/.dotfiles"
alias startrg="cd $HOME/Work/Regrinding/devops/ && tye run"
